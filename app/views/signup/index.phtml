<?php
$form = $this->forms->get('register');
?>

<section class="main-container">
    <div class="main-wrapper">
        <h2>Signup</h2>
        
        <form class="signup-form" method="post" action="/signup/submit">
            <p>
                <label> First Name </label>
                <?php echo $form->render('first_name'); ?>
            </p>
            <p>
                <label> Last Name </label>
                <?php echo $form->render('last_name'); ?>
            </p>
            <p>
                <label> Username </label>
                <?php echo $form->render('username'); ?>
            </p>
            <p>
                <label> Email </label>
                <?php echo $form->render('email'); ?>
            </p> 
            <p>
                <label> Password </label>
                <?php echo $form->render('password'); ?>
            </p> 
            <p>
                <label> Repeat password</label>
                <?php echo $form->render('repeatPassword'); ?>
            </p> 
            
            <button type="submit" name="submit" value="set">Sign Up</button>
        </form>
    </div>
</section>